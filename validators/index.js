const quiz = require('./quiz.validator')
const question =  require('./question.validator');
const answer = require('./answer.validator')
const user  = require('./user.validator')

module.exports = {
    quiz, question, answer, user
}